(function (jQuery, _, M139) {
    var $ = jQuery;
    var superClass = M139.View.ViewBase;
    M139.namespace('M2012.GroupMail.View.Empty', superClass.extend(
    {
        el: "#m139-group-list",
        template: $("#tpl-team-no-group").html(),
        events: {
        },
        initialize: function (options) {
        	this.model = options.model;
            return superClass.prototype.initialize.apply(this, arguments);
        },
        initEvents : function(){
        
        },
		render : function(){
		    $(this.el).html(_.template($("#tpl-team-no-group").html()));
		},
		// 初始化模型层数据
		getDataSource : function(callback){
		
		}
    }));
})(jQuery, _, M139);

